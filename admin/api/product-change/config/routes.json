{
  "routes": [
    {
      "method": "GET",
      "path": "/product-changes",
      "handler": "product-change.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-changes/count",
      "handler": "product-change.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-changes/:id",
      "handler": "product-change.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/product-changes",
      "handler": "product-change.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/product-changes/:id",
      "handler": "product-change.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/product-changes/:id",
      "handler": "product-change.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
